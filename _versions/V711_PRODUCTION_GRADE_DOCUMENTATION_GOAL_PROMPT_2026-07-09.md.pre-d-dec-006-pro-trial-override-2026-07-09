# v7.1.1 Production-Grade Documentation Goal Prompt

Use this as the long-running goal prompt.

```text
Goal: make the Sourcera corpus production-grade end to end: no open P0/P1 defects, no unresolved current-source conflicts, no undocumented product behavior, no stale source-authority routing, no misleading “historical” closures, no unverified runtime-gate promotions, and no hidden commercial-wedge gap.

Current baseline as of 2026-07-09:
- Exact-status right-edge scan: 0 open P0, 0 open P1, 0 blocked P1, 598 open P2, 192 open P3.
- Stamp gate: FAIL expected; 423 runtime rows, 274 runtime_active, 147 runtime-evidence blockers.
- Runtime blockers split: 14 M02.3, 102 M11.3, 26 M21.3, 5 M24.3. Do not call these historical. They require evidence or implementation-pack closure.
- D-DEC-010 and pricing singleton residual cleanup are closed; cost-base drift thresholds are now 7/10/25 with `warning_7_10`.

Binding rules:
1. Read AGENTS.md first. Master Spec wins for product behavior. Pricing companions win only for pricing narrative where Master Spec §34 does not own the number.
2. Use `_audit/V711_BACKLOG_INDEX.md` for live v7.1.1 routing, `_audit/DEFECT_LEDGER.md` for canonical row status, `_audit/REMEDIATION_BACKLOG.md` for cluster/pack routing, `_integration/Decisions.md` for decision conflicts, and `_integration/AUTHORED_EXTENSIONS_LEDGER.md` for AE release gates.
3. Before editing an authoritative file, create a dated backup in `_versions/`.
4. For every gap, classify it as exactly one of: true-live remediation, stale-open status sync, superseded/re-targeted, lower-severity residual, or runtime-evidence blocker. Never close by calling it historical without landing-site proof.
5. If authoring unsupported new product behavior, mark it as an Authored Extension and update the AE ledger.
6. If a spec-tree invariant is closed, add or update a runtime-active detector plus pass/fail fixtures.
7. After every slice, run direct detector proof where applicable, full spec-lint, exact-status scan, stamp gate, and update all affected ledgers/counts.

Priority fix order:
1. Prove live baseline:
   - Run exact-status scan over `_audit/DEFECT_LEDGER.md`.
   - Run full `npm --prefix tools/spec-lint run all -- --no-emit --ae-ledger ../../_integration/AUTHORED_EXTENSIONS_LEDGER.md`.
   - Run `tools/release/stamp_gate.ts --json`.
   - Update `_audit/_tmp/v711_stamp_gate_latest.json`.

2. Release-blocking truth first:
   - Re-check any open/blocked P0/P1. If any exist, stop lower-severity work and remediate them.
   - If none exist, keep the proof current in AGENTS.md, CLAUDE.md, `_audit/V711_BACKLOG_INDEX.md`, and `_integration/RECONCILIATION.md`.

3. Runtime stamp-gate blockers:
   - For each remaining blocker, inspect the §M.5 row evidence column.
   - Promote only rows provable from docs/spec-tree with detector + fixtures.
   - Leave product-code/runtime rows pending with explicit evidence required.
   - Maintain the blocker classification artifact so no pending runtime row is treated as “done.”

4. Decision-divergence lower-severity rows:
   - Work remaining DEC rows in this order: D-DEC-006, D-DEC-009, D-DEC-011, D-DEC-012.
   - For each, read Decisions.md source, current Master Spec body, Appendix I/J/K/C/G/M touchpoints, and affected API/ops sections.
   - Either adopt the decision, or retain current spec with explicit rationale and close the decision. Do not leave both positions live.

5. Product-codebase gates explicitly not touched:
   - Find all rows and docs saying implementation packs remain pending.
   - For each, decide whether documentation can be completed now or whether product/runtime proof is required.
   - If product/runtime proof is required, keep the row open and define exact acceptance evidence: test name, deploy validator, runtime monitor, API test, static scan, or production artifact.

6. Future-work table hardening:
   - For CMEK, field-level PII encryption, custom scoring rubrics, requirement hierarchies, custom pipeline phases, video demos, expanded i18n, and mobile full parity, decide one of:
     - Fully specify now with entities, APIs, state machines, RBAC, retention, residency, audit, errors, webhooks, plan gates, and tests.
     - Or explicitly re-target with version, owner, evidence gate, and no current production claim.
   - Remove any wording that implies production completeness before evidence exists.

7. Version/source hygiene:
   - Resolve header/version conflicts like v7.1.0a vs “Current version: 6.0.0.”
   - Remove active routing to retired KB Engineering Spec, retired Master Summary, and missing `What_is_Sourcera.md`.
   - Preserve historical references only when labeled historical.

8. Commercial wedge clarity:
   - Make the core wedge explicit in the Master Spec and GTM routing: Sourcera is the procurement operating system where buyer evaluation structure and seller KB/agent response compound into a two-sided workflow/data advantage.
   - Ensure the wedge is reflected in product surfaces, onboarding, Hero Moments, pricing narrative, marketplace loops, analytics, and sales/GTM docs.
   - Do not turn GTM copy into engineering authority.

9. Final production-grade pass:
   - Re-run all gates.
   - Re-run exact-status scan.
   - Re-run stamp gate.
   - Update `_audit/V711_BACKLOG_INDEX.md`, `_audit/REMEDIATION_BACKLOG.md`, `_integration/RECONCILIATION.md`, AGENTS.md, and CLAUDE.md.
   - Produce a final verification artifact listing remaining blockers. If any remain, name exact owner/evidence; do not say “zero gaps.”

Completion bar:
- 0 open P0.
- 0 open P1.
- 0 blocked P1.
- Every P2/P3 has either remediation, explicit re-target, or live backlog owner.
- Stamp gate passes, or every remaining failure is explicitly outside documentation and assigned to product/runtime implementation evidence.
- No active source/version conflict remains.
- No future-work item is implied as production-ready without full spec and evidence.
- Commercial wedge is first-class and consistent across product/spec/GTM without overriding engineering contracts.
```
