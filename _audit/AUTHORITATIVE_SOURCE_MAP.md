# Authoritative source map — retired

This old point-in-time map is no longer current. It remains as a stable path for historical links.

Use `Sourcera_Master_Spec.md` for product authority, `AGENTS.md` for repository routing, and the live release scanners for status.

The pre-cleanup map is available at `legacy-import:_audit/AUTHORITATIVE_SOURCE_MAP.md`.
