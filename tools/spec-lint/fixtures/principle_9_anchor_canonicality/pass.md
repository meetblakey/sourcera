# Spec excerpt

See [Principle 9](#3.13-principle-9-surface-simplicity-engine-complexity) for the
surface-simplicity / engine-complexity rule. The canonical anchor is the only
permitted link target.

Note: the alias `#principle-9` is rejected at lint time — but because this is an
inline-code mention (not a link target), the gate must NOT flag it. The gate must
PASS on this fixture.
