# User Organization Attribute Disambiguation Pass Fixture

### 4.2.3 User (Global) {#4.2.3-user-(global)}

| Field | Type | Constraints | Notes |
| :---- | :---- | :---- | :---- |
| `organization_attribute` | String | nullable | WorkOS Predefined Attribute. Directory-provider-supplied organizational unit / department. NOT to be confused with Sourcera Organization (`org_id`). |

The WorkOS `organization_attribute` is private profile metadata for directory display and DSAR export paths.
