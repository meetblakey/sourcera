| KB ROI | KB entries cited in closed bids × win-rate correlation; top-N cited entries with win-rate lift | `kb_citation_in_closed_bid_attributed` (registered under `seller_kb` event family at §22.18 KB Value Capture; catalogued in Appendix G → KB Value Capture Events subsection, NOT in Appendix G → §51 Product Usage Analytics Event Additions — cross-family consumption per §51.1.5 cross-reference table) |

9. KB ROI panel joins `kb_citation_in_closed_bid_attributed` to bid outcome; integration test with 10 synthesized bids.

**`kb_citation_in_closed_bid_attributed` event registration.** *(V13 D-51-013 remediation 2026-05-12.)* Fires when a KBEntry is cited by a bid response that subsequently reaches a closed-bid outcome (won, lost, or withdrawn). Payload schema: `{seller_org_id, kb_entry_id, bid_workspace_id, cited_at, bid_closed_at, bid_outcome ∈ {won, lost, withdrawn}, response_id, citation_count_in_bid, kb_entry_confidence_at_citation, kb_entry_first_cited_in_closed_bid_at (nullable; populated only on first attribution per `kb_entry_id`)}`. Trigger contract: fires exactly once per `(kb_entry_id, bid_workspace_id)` pair at the bid-close transition (§13 Phase 13 → terminal). Idempotency key: `(kb_entry_id, bid_workspace_id)`. The event is referenced by §51.5.3 KB Utilization Panel + §51.5.6 AC-9 integration test (`kb_citation_in_closed_bid_attributed` join to bid outcome with 10 synthesized bids); the prior §51 cross-reference asserting this event was "registered under `seller_kb` event family" was non-binding — V13 makes the registration canonical. Cardinality bound: `kb_entry_id` per-event-row only; PostHog stores as event-level property. DSAR cascade: pseudonymized via standard pattern. Residency: partition by Seller Org's `residency_region` at emit time. CI gate `kb_citation_in_closed_bid_attributed_registered` (§M.5 — new) asserts schema canonicity and idempotency-key uniqueness.

| `kb_citation_in_closed_bid_attributed_registered` | content_consistency | **`runtime_active`** (promoted 2026-07-09; detector `tools/spec-lint/gates/kb_citation_in_closed_bid_attributed_registered.ts`; verified PASS on live Master Spec and pass/fail fixtures) | post-build | Appendix G KB Value Capture block registers the event; schema canonicity per V13 §51 body; idempotency-key uniqueness on `(kb_entry_id, bid_workspace_id)` — closes D-51-013 | M02.3 |
