### M.5 CI Gate Catalog {#appendix-m-ci-gate-catalog}

| Gate ID | Source phase | Runtime status |
| :-- | :-- | :-- |
| `gate_one` | 14.1 | `runtime_active` |

This invariant is enforced by Appendix M.5 `ghost_gate`, which does NOT resolve
to any catalog row — MUST FAIL (orphan inline gate reference).
